{
  "name": "forgejo-mcp-server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --outdir=./dist --target=node --format=esm",
    "build:tsc": "tsc",
    "start": "node dist/index.js",
    "dev": "bun build src/index.ts --outdir=./dist --target=node --format=esm && node dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.4"
  }
}
